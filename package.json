{
  "name": "calc-state-ts",
  "version": "0.2.0",
  "description": "Typescript definitions for all possible Desmos graph states.",
  "main": "index.js",
  "scripts": {
    "build-schema": "ts-to-json --path state.ts --type State > state_schema.json",
    "test-schema": "cd tests && node test_schema.js"
  },
  "author": "fireflame241",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.6.0",
    "ts-to-json": "^1.4.1"
  }
}
